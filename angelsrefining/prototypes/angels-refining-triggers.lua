-- set triggers for the other angels mods
-- set defaults to start with (may change)
  --BASIC TRIGGERS
if angelsmods.smelting then
  angelsmods.trigger.washing_tech= true
else
  angelsmods.trigger.washing_tech= false
end
